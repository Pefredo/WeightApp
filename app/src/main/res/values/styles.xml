<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Custom themes-->
    <style name="ButtonSave" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textColor">@color/colorTransparentWhite</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/button_save</item>
    </style>

    <style name="recyclerScroll">
        <item name="android:padding">8sp</item>
        <item name="android:layout_marginTop">8sp</item>
        <item name="android:scrollbars">none</item>
    </style>
</resources>
